TTCK Apps Script patch - 2026-07-06

Corrected rule:
- DO NOT add +1 hour globally.
- DATA_CK time must come from each Gmail transaction timestamp/content.
- DO NOT scan the whole Gmail mailbox with bare newer_than:10d, because it pollutes DEBUG_ACB with Firebase, ads, order emails, etc.

Required deployed version:
20260706-targeted-gmail-no-debug-noise

Fixes in apps_script_fixed.txt:
- buildGmailSearchQueries_ no longer adds bare newer_than:10d.
- Gmail queries are targeted:
  1) configured sourceEmail + newer_than
  2) accountKeyword + newer_than, default accountKeyword=19337611
  3) ACB + newer_than
  4) "Ghi có" + newer_than
- Added CAI_DAT setting accountKeyword.
- Added isLikelyTransactionEmail_ to silently skip unrelated emails.
- Silent skipped emails no longer increment parseFail and no longer write DEBUG_ACB.
- Kept exact content time repair: ddmmyy-HH:mm:ss / ddmmyy_HH_MM_SS are written exactly into Ngay/Gio/Ngay gio.
- Added repairDataTimes action for admin.

Expected after deploy and Cập nhật 7 ngày:
- LOG_SYNC Query should NOT contain plain `|| newer_than:10d` by itself.
- DEBUG_ACB should no longer show Firebase/ads/order emails.
- If parsedOk is still 0, DEBUG_ACB should show only bank-like candidate emails, making the actual parser reason visible.

Important:
GitHub Pages repo stores the web UI only. The running Gmail sync code is in Google Apps Script behind the /exec URL. Updating this repo alone does not update Apps Script unless clasp/Apps Script API deployment is connected.